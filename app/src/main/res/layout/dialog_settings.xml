<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/card_bg">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ajustes de Sincronización"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/accent_sky"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sincronización Instantánea Push (FCM):"
        android:textColor="#F9FAFB"
        android:textSize="13sp"
        android:textStyle="bold"
        android:layout_marginBottom="6dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="La aplicación recibe alertas en tiempo real al segundo que cambias el archivo Keybox.xml en GitHub. No requiere temporizadores ni consumo de batería."
        android:textColor="@color/text_sub"
        android:textSize="12sp"
        android:layout_marginBottom="20dp" />

    <Button
        android:id="@+id/btnSaveSettings"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Entendido"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="@color/accent_blue" />

</LinearLayout>
